---
import config from '../data/portfolio.config.json';
const { identity, current_focus, tech_stack, micro_logs, cyber_dictionary } = config;
---

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexus Core | The Architect</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-slate-950 text-slate-200 font-sans antialiased min-h-screen">

    <nav class="w-full max-w-4xl mx-auto p-6 flex justify-between items-center text-xs font-mono text-slate-500 border-b border-slate-900/50">
        <a href="/" class="hover:text-white transition flex items-center gap-2">
            <i class="fa-solid fa-chevron-left"></i> SYSTEM_HOME
        </a>
        <span class="uppercase tracking-widest text-emerald-500 animate-pulse">● LIVE_DATA_FEED</span>
    </nav>

    <main class="w-full max-w-4xl mx-auto p-6 md:p-12 space-y-16">

        <section class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center animate-fade-in-up">
            <div>
                <h1 class="text-4xl font-extrabold text-white mb-4">The Architect</h1>
                <p class="text-slate-400 font-light leading-relaxed mb-6">
                    Beyond the code, I am a strategist connecting philosophy with functionality. 
                    This is my digital garden—a collection of tools, thoughts, and definitions.
                </p>
                <div class="grid grid-cols-2 gap-4 text-xs font-mono">
                    <div class="p-3 bg-slate-900/50 border border-slate-800 rounded">
                        <span class="text-slate-500 block mb-1">CURRENT FOCUS</span>
                        <span class="text-indigo-400">{current_focus.status}</span>
                    </div>
                    <div class="p-3 bg-slate-900/50 border border-slate-800 rounded">
                        <span class="text-slate-500 block mb-1">LISTENING TO</span>
                        <span class="text-emerald-400">{current_focus.music}</span>
                    </div>
                </div>
            </div>
            <div class="bg-slate-900/30 border border-slate-800 rounded-xl p-6">
                <h3 class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-4">// TECH_ARSENAL</h3>
                <div class="grid grid-cols-3 gap-4">
                    {tech_stack.map(tool => (
                        <div class="flex flex-col items-center justify-center p-3 hover:bg-slate-800 rounded transition duration-300 group">
                            <i class={`fa-brands ${tool.icon} fa-xl mb-2 ${tool.color} group-hover:scale-110 transition-transform`}></i>
                            <span class="text-[10px] text-slate-400">{tool.name}</span>
                        </div>
                    ))}
                </div>
            </div>
        </section>

        <hr class="border-slate-900" />

        <section>
            <div class="flex items-center gap-3 mb-6">
                <i class="fa-solid fa-seedling text-emerald-500"></i>
                <h2 class="text-xl font-bold text-white">Digital Garden</h2>
            </div>
            <div class="space-y-4 border-l-2 border-slate-900 pl-6 ml-2">
                {micro_logs.map(log => (
                    <div class="relative">
                        <span class="absolute -left-[31px] top-1 w-3 h-3 bg-slate-900 border-2 border-indigo-500 rounded-full"></span>
                        <div class="flex flex-col sm:flex-row sm:items-baseline gap-2 mb-1">
                            <span class="text-xs font-mono text-indigo-400">{log.date}</span>
                            <span class="text-[10px] font-mono border border-slate-800 px-1.5 rounded text-slate-500 uppercase">{log.type}</span>
                        </div>
                        <p class="text-slate-300 font-light text-sm">{log.content}</p>
                    </div>
                ))}
            </div>
        </section>

        <section>
            <div class="flex items-center gap-3 mb-6">
                <i class="fa-solid fa-book-journal-whills text-cyan-500"></i>
                <h2 class="text-xl font-bold text-white">Cyber Dictionary</h2>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {cyber_dictionary.map(item => (
                    <div class="p-4 bg-slate-900/20 border border-slate-800 rounded hover:border-cyan-500/30 transition duration-300 group">
                        <h3 class="text-sm font-bold text-cyan-400 font-mono mb-1 group-hover:text-cyan-300">
                            > {item.term}
                        </h3>
                        <p class="text-xs text-slate-400 italic">"{item.def}"</p>
                    </div>
                ))}
            </div>
        </section>

    </main>

    <footer class="mt-12 py-8 text-center border-t border-slate-900">
        <p class="text-[10px] font-mono text-slate-600">NEXUS PROTOCOL v5.0 // CREATED BY SAZID</p>
    </footer>

</body>
</html>
